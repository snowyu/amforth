# -----------------------------------------------------------------------------
  CODEWORD Flag_visible, "sp@", SP_FETCH # ( -- a-addr )
# -----------------------------------------------------------------------------
  savetos
  mv x3, x4
  NEXT
# -----------------------------------------------------------------------------
  CODEWORD Flag_visible, "sp!", SP_STORE # ( a-addr -- )
# -----------------------------------------------------------------------------
  mv x4, x3
  loadtos

  NEXT

USER "sp", SP, USER_SP
USER "sp0", SP0, USER_SP0
